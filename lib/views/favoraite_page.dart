import 'package:flutter/material.dart';

class FavoraitePage extends StatelessWidget {
  const FavoraitePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Favoraite Page'),
      ),
    );
  }
}
